# Kafka Configuration
# Copy file này thành .env và cập nhật giá trị phù hợp với môi trường của bạn

# IP của WSL2 - Lấy bằng lệnh: hostname -I (trong WSL2)
# Ví dụ: 172.27.34.172
WSL2_IP=localhost

# Kafka Port
KAFKA_PORT=9092

# Kafka Topic
KAFKA_TOPIC=house-listings

# Số lượng tin đăng cần crawl (mặc định: 50)
CRAWL_LIMIT=50

# Batch size để flush messages (mặc định: 10)
BATCH_SIZE=10
